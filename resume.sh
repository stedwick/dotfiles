export RESUME_APP_ROOT="$RESUME_ROOT/resume-app"
export RESUME_WEB_ROOT="$RESUME_ROOT/resume-web"
export RESUME_DB_ROOT="$RESUME_ROOT/resume-db"

[ -r "$HOME/.resume.secret.sh" ] && source "$HOME/.resume.secret.sh"

