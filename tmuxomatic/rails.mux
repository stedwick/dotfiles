window dev

0

dir $RAILS_ROOT

0 run clear && git status && git branch && git tag

window etc

0

dir $RAILS_ROOT

0 run clear && git status && git branch && git tag

window serv

12
13
14
15

dir $RAILS_ROOT
5 foc

1 run bundle exec rails server
2 run sleep 5 && browser-sync start --proxy 'localhost:3000' --files 'app'
3 run sleep 10 && ngrok http 3001
4 run docker run -it --rm --name adminer -p 8080:8080 adminer
5 run bundle exec rails console

